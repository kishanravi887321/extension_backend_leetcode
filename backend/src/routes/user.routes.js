import { loginUser, googleLogin, refreshToken, logout, twoFactorAuth, accessBy2faForGuest } from "../controllers/user.controllers.js";
import { authenticateToken } from "../middleware/auth.middleware.js";
import express from "express";
const router = express.Router();
router.post("/login", loginUser);
router.post("/google-login", googleLogin);
router.post("/refresh-token", refreshToken);
router.post("/logout", logout);
router.post("/2fa", authenticateToken, twoFactorAuth);
router.post("/guest-login", accessBy2faForGuest);
export { router };